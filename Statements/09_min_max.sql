-- SOLO TE DEVUELVE UNA COLUMNA, DE MOMENTO SE QUEDA ASI

SELECT MAX(age) FROM users

# MAX(age)
69

SELECT MIN(age) FROM users
-- te devuelve una fila aunque haya dos registros con edad 20
# MIN(age)
20


